<!DOCTYPE html>
<html>
<head>
<style>

body {
  padding: 20px;
  font-family: sans-serif;
  background: #f2f2f2;
}
img {
  width: 33%; /* need to overwrite inline dimensions */
  height: auto;
}
h1 {
  margin-bottom: .5em;
}
.grid-container {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(360px, 1fr));
}





</style>
</head>
<body>
<h1 align="center">Welcome to the ImageRepo</h1>


<br><br>


<div class="grid-container">
    <article class="image-pic">
        <% @pictures.each_slice(3) do |pics| %>
            <% pics.each_with_index do |img, index| %>
                <div class="grid-<%= index + 1%>">
            <!--<a class="image-title" href="#">San Francisco</a>
            <div class="image-image">-->
                    <a href="#">
                        <img src="<%= (url_for(img.image)) %>">
                    </a>
                </div>
            <% end %>         
        <% end %>
    </article>
</div>

</body>
</html>